{
    "name": "@material-bluemap/parts",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "Splits an oversized release asset into fixed-size parts and puts it back together byte for byte",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "test": "vitest run",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    }
}
