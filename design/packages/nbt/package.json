{
    "name": "@worldlens/nbt",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "BlueNBT-subset NBT reader/writer with schema mapping",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "test": "vitest run",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    }
}
