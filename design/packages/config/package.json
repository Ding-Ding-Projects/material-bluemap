{
    "name": "@material-bluemap/config",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "BlueMap configuration schema, HOCON reader/writer and CLI flag model",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "test": "vitest run",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "zod": "^4.4.3"
    }
}
